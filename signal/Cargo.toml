[package]
name = "signal"
version = "0.1.0"
edition = "2024"

[lib]
path = "./src/signal.rs"

[dependencies]
model = { path = "../model" }
log = "0.4"
chrono = "0.4"
argmin = { version = "0.11.0", features = ["_ndarrayl", "rayon"] }
ndarray = "0.16"
